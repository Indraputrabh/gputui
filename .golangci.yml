linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - gocritic
    - misspell
    - gofmt

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
